!!! 5
%html
	%head
		%g:set(var="entityName" value="${message(code: 'location.label', default: 'Location')}")/
		%title
			%g:message(code='default.list.label' args='[entityName]')/
	%body
		.content.scaffold-list(role="main")
			%h1.ui-widget-header.ui-corner-all
				%g:message(code="default.list.label" args="[entityName]")/
			%table.list.ui-widget.ui-widget-content.ui-corner-all
				%thead
					%tr
						%g:sortableColumn(property="name" title="${message(code: 'location.name.label', default: 'Name')}")/
				%tbody
					%g:each(in="${locationInstanceList}" status="i" var="locationInstance")
						%tr(class="${(i % 2) == 0 ? 'even' : 'odd'}")
							%td
								%g:link(action="show" id="${locationInstance.id}") ${fieldValue(bean: locationInstance, field: "name")}
			%div(class="pagination")
				%g:paginate(total="${locationInstanceTotal}")/

		%div(style="padding-top:1em;")
			%button.jq-button(onclick="location.href='${createLink(action: 'create')}'")
				%g:message(code="default.new.label" args="[entityName]")/